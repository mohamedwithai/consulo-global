// Palette values for JavaScript consumers (framer-motion style props, canvas,
// inline SVG). Tailwind classes should use the `charcoal` / `signal` / `bone`
// tokens in tailwind.config.js — these two must stay in step.
export const COLORS = {
  charcoal: '#3F4143',
  charcoalDeep: '#2A2C2E',
  signal: '#FFD91A',
  signalDeep: '#E6C200',
  bone: '#F7F7F5',
};
