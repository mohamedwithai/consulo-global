# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*.local
.env*

# Resend API key for the contact form — never commit real credentials.
# config.sample.php (the template) IS tracked; config.php (with the real
# key) is created manually on the server and must stay untracked.
public/api/config.php

